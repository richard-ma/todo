matrix:
  include:
    - language: python
      python: 3.5
      install:
        - pip install -r backend/requirements.txt
      before_script:
        - cd backend
      script:
        - python manager.py test
